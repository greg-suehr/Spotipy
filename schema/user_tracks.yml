# The Spotify Web API exposes an explicit genre classification for artists

name: user_tracks
columns:
  user_track_id:
    datatype: INTEGER
    key: PRIMARY
  username:
    datatype: TEXT
  track_uri:
    datatype: TEXT
  playlists:
    datatype: INTEGER
  saved:
    datatype: INTEGER
  saved_date:
    datatype: TEXT
  is_heat_rock:
    datatype: INTEGER
  last_played_date:
    datatype: TEXT
  created_date:
    datatype: TEXT
  expire_date:
    datatype: TEXT